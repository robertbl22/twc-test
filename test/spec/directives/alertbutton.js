'use strict';

describe('Directive: alertButton', function () {

  // load the directive's module
  // beforeEach(module('twcTestApp'));

  // var element,
  //   scope;

  // beforeEach(inject(function ($rootScope) {
  //   scope = $rootScope.$new();
  // }));

  // it('should make hidden element visible', inject(function ($compile) {
  //   element = angular.element('<alert-button></alert-button>');
  //   element = $compile(element)(scope);
  //   expect(element.text()).toBe('this is the alertButton directive');
  // }));
});
